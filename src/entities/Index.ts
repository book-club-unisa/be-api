import { Book } from './Book';
import { User } from '../User/user.entity';
import { Bookclub } from './Bookclub';

const entities = [User, Book, Bookclub];
export { User, Book, Bookclub };

export default entities;
